# [Tool Name]

One-line description.

## Usage

```bash
# fill in once the tool exists
```

## Requirements

- GrapheneOS device (or specify if stock AOSP/Android also supported)
- Root: not required (update if this project is the exception)
- [Other dependencies]

## Notes

See `CONTEXT.md` in this folder for build context, and the workspace root's
`AGENTS.md` for the GrapheneOS constraints every tool here follows.
